import Header from '../components/common/Header';

const HomePage = () => {
    return (
        <div>
            <Header />
            <p>This is HomePage.</p>
        </div>
    );
};

export default HomePage;